<p>This document collects ideas that have been thought through but not built into the core design. The rest of the corpus is the load-bearing material; this page is direction, not commitment. Each section sketches what the capability is, where it would fit, the concrete technology choices that seem appropriate, and what would need to be designed before a build.</p>
<ac:structured-macro ac:name="info"><ac:rich-text-body><p><strong>In plain terms.</strong> a vendor or future team picking up the platform will eventually ask "what's next?" These are the sketches that answer that question, ordered so the most natural extensions come first.</p></ac:rich-text-body></ac:structured-macro>
<p>Ordered roughly by ease-of-fit with the existing platform — early entries plug into existing patterns with minimal change; later entries introduce new substrates, new cost shapes, or new operational concerns.</p>
<h1>1. Semantic dataset discovery — OpenSearch + Bedrock Knowledge Base</h1>
<p>The STAC catalogue handles structured discovery: spatial extent, temporal extent, declared keywords. It does not handle <em>meaning</em>. A user asking "what data do we have on coastal erosion" wants a result they would not find by keyword match alone — the relevant dataset might be called "Shoreline Recession Polygons 2024" and never use the word "erosion".</p>
<p>Augmenting the catalogue with a semantic search layer closes that gap. <strong>Amazon OpenSearch Serverless</strong> holds an index of every dataset's description, lineage, attribution, and schema fields. <strong>Amazon Bedrock Knowledge Base</strong> manages embeddings and the retrieval contract over that index, exposing semantic search as a single API the discovery agent (or any client) can call. The STAC API stays as it is; semantic discovery is a sidecar over the same dataset registry.</p>
<ac:structured-macro ac:name="info"><ac:rich-text-body><p><strong>In plain terms.</strong> a sidecar to STAC that lets users (and agents) find datasets by what they <em>mean</em>, not just what they're labelled.</p></ac:rich-text-body></ac:structured-macro>
<p><strong>Concrete tech to consider.</strong> OpenSearch Serverless, Bedrock Knowledge Base, Bedrock embedding models. Index refresh tied to the editing pipeline's promotion step so new and updated datasets are searchable within minutes.</p>
<p><strong>What needs designing.</strong> The indexing pipeline (which fields contribute to the embedding, how schema descriptions are summarised). Permission-aware retrieval — KB results must filter by the user's <code>X-Auth-User-Datasets</code> (see <ac:link><ri:page ri:content-title="03 — Authorisation"/><ac:plain-text-link-body><![CDATA[03 Authorisation]]></ac:plain-text-link-body></ac:link> for the header contract), not just return everything. Handling of dataset deprecation and retirement so the index stays in sync with the catalogue's lifecycle states. The cost shape: OpenSearch Serverless has a minimum capacity floor, so this adds always-on cost the rest of the platform avoids.</p>
<h1>2. Geocoding and address search</h1>
<p>Forward geocoding ("14 Smith St" → coordinates), reverse geocoding (nearest address to a point), and proximity queries ("features within 200m of this location"). The platform currently has no first-party geocoder; users either consume external services or go without.</p>
<p>OpenSearch's <code>geo_point</code> and <code>geo_shape</code> field types make this a one-index problem. With an authoritative address dataset ingested, all three operations are standard OpenSearch queries.</p>
<p><strong>Why it fits.</strong> The auth gate (see <ac:link><ri:page ri:content-title="03 — Authorisation"/><ac:plain-text-link-body><![CDATA[03 Authorisation]]></ac:plain-text-link-body></ac:link>) fronts every backend; OpenSearch sits behind it like any other service. No external geocoder calls — useful for jurisdictions where queries against third-party services would leak data location.</p>
<p><strong>Concrete tech to consider.</strong> OpenSearch Serverless (shared with the semantic-discovery index above), <code>geo_point</code> for address points, <code>geo_shape</code> for polygon overlays (council areas, suburbs, flood zones — anything you want to combine with text+spatial queries).</p>
<p><strong>What needs designing.</strong> The address-normalisation pipeline is the real work: canonical address fabric → indexable documents with consistent CRS, deduplication, alias handling. The serving side is well-trodden in the industry but has not been exercised in this platform. Versioning of the address dataset (point-in-time queries against historical addresses) is a deeper concern that may or may not be in scope.</p>
<h1>3. Point cloud serving — COPC + auth proxy</h1>
<p>LiDAR datasets are typically too large to serve as one file and too detailed to render naively. <strong>Cloud Optimised Point Clouds (COPC)</strong> solve this: a LAZ file reorganised as a clustered octree with a header-resident spatial index. Clients (CesiumJS) request only the octree nodes relevant to their viewport and zoom via HTTP byte ranges — coarse overview first, finer detail as the user dwells.</p>
<p>The platform's role is the same as for PMTiles: store the file in S3, gate access via the auth Lambda, let the client drive the streaming. A thin proxy validates the token, checks dataset permissions in DynamoDB, and forwards byte ranges to S3.</p>
<ac:structured-macro ac:name="info"><ac:rich-text-body><p><strong>In plain terms.</strong> the same byte-range + auth-proxy pattern that already works for PMTiles, applied to LiDAR. The proxy does not need to understand the format — only gate access to it.</p></ac:rich-text-body></ac:structured-macro>
<p><strong>Concrete tech to consider.</strong> PDAL + Entwine (one-time batch per dataset) to convert raw LAZ to COPC, AWS Batch (or a one-off Fargate task following the operator-driven pattern in <ac:link><ri:page ri:content-title="08 — Raster Services"/><ac:plain-text-link-body><![CDATA[08 Raster Services]]></ac:plain-text-link-body></ac:link> — choose based on whether builds are recurring or one-shot), STAC's <code>pointcloud</code> extension for catalogue entries linked through <ac:link><ri:page ri:content-title="10 — Discovery"/><ac:plain-text-link-body><![CDATA[10 Discovery]]></ac:plain-text-link-body></ac:link>, CesiumJS as the client renderer (also see §4).</p>
<p><strong>What needs designing.</strong> None of this has been built end-to-end. The ingestion pipeline: raw LAS/LAZ on S3 → PDAL normalise (consistent CRS, classification schema, LAS version) → Entwine build → COPC output → STAC item. Existing LiDAR holdings should be audited before the pipeline is written — inconsistent CRS and mixed classification schemes are common and require explicit normalisation steps. Permission gating: byte-range proxy versus presigned URLs with embedded auth scope (presigned URLs are simpler but cannot be revoked once issued). Retention and lifecycle for raw versus COPC files.</p>
<h1>4. 3D rendering with Cesium / TerriaJS</h1>
<p>A 3D companion to the 2D MapLibre map client described in <ac:link><ri:page ri:content-title="15 — Map Client (Web Application)"/><ac:plain-text-link-body><![CDATA[15 Map Client]]></ac:plain-text-link-body></ac:link>. <strong>TerriaJS</strong>, running on <strong>CesiumJS</strong>, renders point clouds (COPC natively), COGs draped over terrain, time-dynamic data, and routing geometry on actual terrain. Shares the dataset catalogue and auth gate with the rest of the platform.</p>
<p>A 3D view is the natural primary surface for some tasks — flythrough of LiDAR, terrain-aware route inspection, infrastructure planning against elevation. The 2D MapLibre client and the 3D Cesium/TerriaJS shell coexist; users choose based on the task.</p>
<p><strong>Why it fits.</strong> Every data format the platform produces (COG, COPC, MVT, GeoJSON, MosaicJSON) is consumable by CesiumJS in principle. The STAC catalogue is the entry point. The auth gate is unchanged. None of this has been wired up in this platform — the <em>principle</em> of fit is sound; the integration work is undone.</p>
<p><strong>Concrete tech to consider.</strong> TerriaJS as the catalogue/visualisation shell, CesiumJS as the render engine. TerriaJS catalogue items support polling GeoJSON endpoints with a <code>refreshInterval</code> for semi-live data; WebSocket CZML for genuinely live operational feeds.</p>
<p><strong>What needs designing.</strong> Shared session and auth between the 2D and 3D clients so users do not sign in twice. Feature parity for editing — drawing and attribute editing in 3D is a UX problem this design has not approached. Layer-state synchronisation if the user toggles between 2D and 3D views.</p>
<h1>5. Computer vision inference proxy</h1>
<p>A thin FastAPI proxy that fetches an imagery tile from the raster tile server, runs inference (building detection, vehicle detection, vegetation classification), converts pixel-space detections back to geographic coordinates via standard XYZ tile math, and returns a GeoJSON FeatureCollection. MapLibre renders the detections as an overlay layer.</p>
<p>The proxy pattern is the same as the WMTS proxy and the Coverages API described in <ac:link><ri:page ri:content-title="08 — Raster Services"/><ac:plain-text-link-body><![CDATA[08 Raster Services]]></ac:plain-text-link-body></ac:link>: gate the request, do the work, return geospatial data. Detection output is GeoJSON, which composes with everything else on the platform — including the query layer's spatial operations and the <code>saveQueryResult</code> mutation in <ac:link><ri:page ri:content-title="07 — Query Layer"/><ac:plain-text-link-body><![CDATA[07 Query Layer]]></ac:plain-text-link-body></ac:link>, and the editing pipeline in <ac:link><ri:page ri:content-title="11 — Editing Pipeline"/><ac:plain-text-link-body><![CDATA[11 Editing Pipeline]]></ac:plain-text-link-body></ac:link> so a user could promote detection output into a managed dataset.</p>
<p><strong>Concrete tech to consider.</strong> Samgeo (SAM fine-tuned for geospatial output) handles georeferencing internally and returns GeoJSON natively. Small SAM (<code>vit_b</code>) runs acceptably on CPU; for interactive response times, either ECS on EC2 with GPU instances or SageMaker async inference — Fargate does not currently expose GPU. ElastiCache keyed on <code>(tile_coordinates, detection_type, parameters)</code> so the same tile does not pay inference twice. The auth gate decides who may call detection at all — it is a permission-gated capability, not an open one.</p>
<p><strong>What needs designing.</strong> A minimum-zoom guard (small SAM needs zoom 17–19 for building-level detail from typical aerial imagery; allowing detection at lower zoom wastes compute and returns garbage). A parameter API — the raw thresholds (<code>pred_iou_thresh</code>, <code>points_per_side</code>, <code>min_mask_region_area</code>, <code>stability_score_thresh</code>) are exposed for advanced users; an agent maps natural-language intent ("only large buildings") to those parameters. The cost trade-off between CPU and GPU instances. Output schema: confidence score in each feature's properties for client-side filtering.</p>
<h1>6. Multi-agent orchestration — Bedrock AgentCore</h1>
<p>The agentic postscript at the end of <ac:link><ri:page ri:content-title="10 — Discovery"/><ac:plain-text-link-body><![CDATA[10 Discovery]]></ac:plain-text-link-body></ac:link> describes the simple shape — one agent over the GraphQL surface, with STAC providing catalogue context. This section describes the elaborate shape: a <strong>supervisor agent</strong> that orchestrates specialist sub-agents, each owning a domain.</p>
<ac:structured-macro ac:name="code"><ac:plain-text-body><![CDATA[Supervisor (Bedrock AgentCore)
  ├── Discovery Agent       — Bedrock KB + STAC + catalogue queries
  ├── Cartography Agent     — assembles MapLibre styles, calls tile/feature APIs
  ├── Spatial Query Agent   — OpenSearch, Valhalla, the GraphQL query layer
  └── CV Agent              — calls the CV inference proxy, interprets results]]></ac:plain-text-body></ac:structured-macro>
<p><strong>Bedrock AgentCore</strong> provides session memory across conversation turns (so a user can refine — "now add flood zones to that map"), tool-execution lifecycle management, KB integration, <strong>user-token passthrough so the agent never has more permission than the user it serves</strong> (see <ac:link><ri:page ri:content-title="03 — Authorisation"/><ac:plain-text-link-body><![CDATA[03 Authorisation]]></ac:plain-text-link-body></ac:link> for the token-to-headers contract), and an audit trail of agent actions. The audit trail is non-optional for regulated deployments.</p>
<p><strong>Why it fits.</strong> Every sub-agent is just another authenticated client of an existing platform API. The supervisor coordinates; the sub-agents have narrow tool schemas; the platform does what it already does. Same auth gate, same data, same cost shape per query.</p>
<p><strong>Concrete tech to consider.</strong> Amazon Bedrock AgentCore for the orchestration runtime, Bedrock Knowledge Base for catalogue retrieval, the OpenSearch + KB combination from §1, the existing platform APIs as tools.</p>
<p><strong>What needs designing.</strong> Prompts and tool schemas for each specialist agent. KB indexing schema (what fields contribute to the embedding for "find me a dataset like…"). Permission-aware response shaping — the discovery agent must not describe datasets the user cannot access. Fan-out guardrails (an agent that wants to compose fifty isochrones should be bounded; the GraphQL query layer's batch limit from <ac:link><ri:page ri:content-title="07 — Query Layer"/><ac:plain-text-link-body><![CDATA[07 Query Layer]]></ac:plain-text-link-body></ac:link> provides a server-side floor, but the agent should reason about its own budget). Evaluation — how do you regression-test an agent? Conversational UX patterns for refinement and back-tracking.</p>
<h1>7. Field data capture</h1>
<p>A field-staff app for capturing GPS points, photographs, and form data — the role currently filled by tools like Esri Survey123. Offline-capable (field staff work where connectivity is unreliable), GPS-aware (uses device location), schema-driven (the same dataset JSON Schemas drive the form rendering).</p>
<p>The platform's existing editing pipeline absorbs the submissions: each field outing is an edit session (or a batch of sessions, one per submission), routed through the standard validation → generation → promotion path described in <ac:link><ri:page ri:content-title="11 — Editing Pipeline"/><ac:plain-text-link-body><![CDATA[11 Editing Pipeline]]></ac:plain-text-link-body></ac:link>.</p>
<p><strong>Why it fits.</strong> Schema-driven editing is already a pattern in <ac:link><ri:page ri:content-title="15 — Map Client (Web Application)"/><ac:plain-text-link-body><![CDATA[15 Map Client]]></ac:plain-text-link-body></ac:link> via RJSF. Per-dataset permissions, validation rules, and review workflow all apply unchanged. A field app is a new front-end against existing back-end APIs.</p>
<p><strong>Concrete tech to consider.</strong> Form.io offers an offline-capable form runtime with a familiar form-builder UX — note this is the one explicitly non-AWS substrate suggested in this document; alternatives include a custom React Native app or a Capacitor wrapper around the existing map client. Server-side spatial enrichment on submit: reverse-geocode the captured point against the address dataset (§2), point-in-polygon for council lookup, automatic attachment of the nearest road network feature.</p>
<p><strong>What needs designing.</strong> Offline reconciliation (last-write-wins, conflict review on sync, or queue-and-prompt). Trip-based session model — does each field outing become one edit session, or one session per submission? Spatial enrichment hooks (which datasets get queried at submit time; how the result attaches to the feature). Photograph handling (S3 upload via presigned URL, EXIF orientation, thumbnails). Form versioning when a dataset's schema changes mid-trip.</p>
<h1>8. Automated report generation</h1>
<p>An agent assembles a written assessment — site context, recent imagery, historical change, surrounding land use, flood and bushfire risk — into a formatted document. The inputs are platform API calls; the work is composition.</p>
<p><strong>Why it fits.</strong> Every input is already a platform API. The CV proxy (§5) adds detection-derived context. The history queries in <ac:link><ri:page ri:content-title="11 — Editing Pipeline"/><ac:plain-text-link-body><![CDATA[11 Editing Pipeline]]></ac:plain-text-link-body></ac:link> provide change-over-time. The query layer composes spatial operations. An agent is the natural orchestrator. None of this has been built, however; the path described below is unproven.</p>
<p><strong>Concrete tech to consider.</strong> The multi-agent setup from §6 as the orchestration layer, an ODF or DOCX layout template (ODF is open-format; DOCX has broader installed-base support), headless map rendering for the figures (a small Fargate service that takes a bbox, style, and dimensions and returns a PNG), Bedrock for the prose generation.</p>
<p><strong>What needs designing.</strong> Report templates — the layout, section structure, which data sources feed which sections per assessment type. The agent's planning prompt — how it decides which datasets to include for a given query. Human-in-the-loop checkpoint — does the draft go to a reviewer before delivery, or directly to the requester? Output format choice (ODF for editability, PDF for finality, both?). Audit trail — every figure and claim should be traceable to the source dataset and version.</p>
<h1>9. Spatial subscriptions</h1>
<p>A user defines an area of interest — polygon, bbox, or point-and-radius — and a dataset they care about. When new features are added or existing features change within that area, they receive a notification.</p>
<p><strong>Why it fits.</strong> Edits flow through the editing pipeline, which already writes an event log to <code>metadata/dataset_events/</code> (see <ac:link><ri:page ri:content-title="11 — Editing Pipeline"/><ac:plain-text-link-body><![CDATA[11 Editing Pipeline]]></ac:plain-text-link-body></ac:link>). A subscription manager listens on those events, filters them against registered AOIs, and dispatches notifications.</p>
<p><strong>Concrete tech to consider.</strong> DynamoDB for subscriptions (partition key <code>USER#user_id</code>, sort key <code>SUB#sub_id</code>, AOI stored as GeoJSON in the item). Event-driven Lambda that filters new feature events against AOIs (point-in-polygon for the simple case; a pre-indexed AOI set for scale). Amazon SES or SNS for email and SMS channels; an in-app inbox in the map client (§15) for users who prefer not to receive external messages.</p>
<p><strong>What needs designing.</strong> AOI matching at scale — per-event point-in-polygon against all subscriptions is fine for hundreds of subscriptions; not for hundreds of thousands. A pre-built AOI spatial index becomes necessary. Notification batching so a bulk update of 1,000 features does not fire 1,000 emails. Client surface for managing subscriptions, snoozing, channel selection. Permissioning — a subscription should not surface features the user cannot otherwise see (see <ac:link><ri:page ri:content-title="03 — Authorisation"/><ac:plain-text-link-body><![CDATA[03 Authorisation]]></ac:plain-text-link-body></ac:link> for the dataset-access contract).</p>
<h1>10. Change detection</h1>
<p>Scheduled comparison of new versus previous aerial captures. The CV inference proxy (§5) runs on both, the differences are computed (features present in the new capture and not the previous, or vice versa), and the result is written as a <code>change_detection_alerts/</code> GeoJSON layer. Users with a spatial subscription (§9) covering the affected area are notified.</p>
<p><strong>Why it fits.</strong> Captures already arrive on a schedule — the operator-driven COG ingest tooling in <ac:link><ri:page ri:content-title="08 — Raster Services"/><ac:plain-text-link-body><![CDATA[08 Raster Services]]></ac:plain-text-link-body></ac:link> covers the input side. The CV proxy already produces GeoJSON. The subscription mechanism filters by AOI. Compose them.</p>
<p><strong>Concrete tech to consider.</strong> EventBridge-scheduled Lambda that triggers per-tile inference on both captures, the existing CV pipeline, the same notification channel as subscriptions.</p>
<p><strong>What needs designing.</strong> False-positive handling — CV produces noise, especially around tree canopy, shadow movement, vehicle parking. What threshold filters output before users see alerts? The comparison logic itself — true geometric diff via spatial ST_Difference, or detection-presence diff (a building was detected here last year, not this year)? Feedback loop where reviewers mark false positives and the system learns its thresholds.</p>
<h1>11. Live data — GTFS Realtime and sensor integration</h1>
<p>The platform is batch-oriented today: edits land, the pipeline runs, tiles update on a cadence of minutes. A live data surface — vehicle positions, environmental sensor readings, flood gauge levels — would extend it with cadence in the order of seconds, suiting use cases the batch model does not (real-time traffic overlays, live emergency dashboards).</p>
<p><strong>Why it fits.</strong> OpenSearch's <code>geo_point</code> indexing supports high-cadence updates. The map client (15) already supports GeoJSON sources with a polling <code>refreshInterval</code>, so a polled OpenSearch endpoint surfaces live data through the existing client without front-end changes.</p>
<p><strong>Concrete tech to consider.</strong> EventBridge-scheduled Lambda polling external feeds (GTFS Realtime, IoT sensor APIs, hydrology services) and writing to OpenSearch with timestamp and source attribution. Map client polls the OpenSearch endpoint at the desired cadence. For genuinely sub-second data (rare in this domain), a WebSocket fan-out becomes appropriate — but note this introduces a <em>different</em> gateway (API Gateway WebSocket APIs or AppSync) and a different auth flow than the rest of the platform's HTTP-API gate; the auth model would need a parallel design.</p>
<p><strong>What needs designing.</strong> A different cost shape than the rest of the platform — live ingest implies always-on compute, even if low-volume. Retention policy: keep all historical readings (large, expensive), or only the most recent per source (cheap, no replay)? Time-window queries ("last 5 minutes of bus positions") in OpenSearch. UX for live overlays — animation versus snapshot, opacity for older readings, separate styling from authoritative data. Authentication: many external live feeds need credentials of their own that the platform must rotate and protect.</p>
<h1>What is solid, what needs work</h1>
<p>To borrow the framing from <ac:link><ri:page ri:content-title="10 — Discovery"/><ac:plain-text-link-body><![CDATA[10 Discovery]]></ac:plain-text-link-body></ac:link>:</p>
<p><strong>Solid.</strong> None of it. Treat every section as a starting point.</p>
<p><strong>Designed but not built.</strong> All eleven. Each section is well enough thought through to brief an architect; none has had code written against it as part of this platform's prototype.</p>
<p><strong>Where the work is heaviest.</strong> The first three (semantic discovery, geocoding, point clouds) extend patterns and substrates the platform already uses — moderate effort, fits naturally. The middle three (3D, computer vision, agents) introduce new substrates with their own operational shapes. The last five (field capture, reports, subscriptions, change detection, live data) introduce new workflows with substantial product design work alongside the engineering.</p>
<p>These are doors, not roads. A future team reading this set should treat each section as a starting point — enough to spark a design conversation, not enough to commit to a build without one.</p>
<h1>A note on dates</h1>
<p>The research that informed this corpus began in November 2025 and the corpus itself was compiled in May 2026 — see <ac:link><ri:page ri:content-title="Document index and reading paths"/><ac:plain-text-link-body><![CDATA[Timeline and currency]]></ac:plain-text-link-body></ac:link> on the document index for the full arc. The ideas in this chapter were sketched within that window. Several of them depend on AWS services and open-source projects that were themselves new at the time of writing: Bedrock AgentCore had only recently reached general availability; OpenSearch Serverless's geospatial story was still maturing; Aurora Serverless v2's scale-to-zero arrived mid-window; mermaid macro vendors in Confluence Cloud were in flux.</p>
<p>Cloud-service landscapes move on the order of months. By the time anyone picks up this set, several of the technology callouts here will have shifted — service names may change, pricing models may evolve, new substrates may emerge, and the gaps these sketches address may have closed (or new gaps opened in their place). <strong>The shape of each sketch is more durable than the specific tech named within it.</strong> Re-check the canonical sources in <ac:link><ri:page ri:content-title="18 — Glossary and References"/><ac:plain-text-link-body><![CDATA[18 Glossary and References]]></ac:plain-text-link-body></ac:link> before committing to a build path, and treat the dating in each section as one factor among several when judging fit.</p>
<p>If you find yourself reading this set more than eighteen months after its compilation date, the right next step is to look at what has changed in the underlying services since, not to take any single technology recommendation at face value. The design choices in <ac:link><ri:page ri:content-title="15 — Design Decisions"/><ac:plain-text-link-body><![CDATA[16 Design Decisions]]></ac:plain-text-link-body></ac:link> — the <em>why</em> of each call — age more slowly than the <em>what</em>.</p>
